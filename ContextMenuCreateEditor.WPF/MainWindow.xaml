<ui:FluentWindow
    x:Class="ContextMenuCreateEditor.WPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ContextMenuCreateEditor.WPF.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ContextMenuCreateEditor.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:ContextMenuCreateEditor.WPF.ViewModels"
    Title="Context Menu Create Editor"
    Width="900"
    Height="560"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Context Menu Create Editor" />

        <Grid Grid.Row="1" Margin="12,8,12,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox
                Grid.Column="0"
                Icon="Search24"
                IconPlacement="Left"
                PlaceholderText="Поиск по имени, файлу или расширению..."
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <CheckBox
                Grid.Column="1"
                Margin="12,0,0,0"
                VerticalAlignment="Center"
                Content="Показывать системные"
                IsChecked="{Binding ShowSystemItems}" />
        </Grid>

        <ScrollViewer
            Grid.Row="2"
            Margin="8,4,8,4"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredView}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Left" Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ShellNewItemViewModel}">
                        <controls:PreviewItem
                            Width="280"
                            DeleteRequested="PreviewItem_DeleteRequested"
                            EditRequested="PreviewItem_EditRequested" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="12,4,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button
                Grid.Column="1"
                Margin="0,0,8,0"
                Appearance="Primary"
                Command="{Binding AddCommand}"
                Content="Добавить" />
            <ui:Button
                Grid.Column="2"
                Margin="0,0,8,0"
                Command="{Binding BackupCommand}"
                Content="Резервная копия" />
            <ui:Button
                Grid.Column="3"
                Command="{Binding RestartExplorerCommand}"
                Content="Перезапустить проводник" />
        </Grid>
    </Grid>
</ui:FluentWindow>
